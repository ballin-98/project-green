export default function Home() {
  return (
    <html lang="en">
      {/* Add the <html> tag with the lang attribute */}
      <body>
        {/* Add the <body> tag */}
        <h1>Hello there :)</h1>
      </body>
    </html>
  );
}
